import React from 'react'
import './News.css';
import NewsItem from '../NewsItem/NewsItem';

function News(props) {
  return (
    <div className='news'>
      <h1>{props.just}</h1>
      <h1>{props.just}</h1>
      <h1>{props.just}</h1>
      <h1>{props.just}</h1>
    </div>
  )
}

export default News
